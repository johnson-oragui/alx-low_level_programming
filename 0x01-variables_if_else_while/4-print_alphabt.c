#include <stdio.h>

/**
 *  main - Entry point
 *
 *  Return: Always 0 (success)
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
	{
		if ((c != 'q' && c != 'e') && c <= 'z')
			putchar(c);
	}
		putchar('\n');

	return (0);
}
