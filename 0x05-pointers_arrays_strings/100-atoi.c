#include "main.h"
#include <stdlib.h>
#include <stdio.h>

/**
 * _atoi - convert a string to an integer.
 *
 * @s: string to convert
 *
 * Return: 0 if there are no numbers in the string
 */
int _atoi(char *s)
{
	int to_num = atoi(s);

	return (to_num);
}
